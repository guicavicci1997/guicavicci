<%= form_for @contato do |f|%>

  <% if @contato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contato.errors.count, "error") %> prohibited this contato from being saved:</h2>

      <ul>
      <% @contato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
  <div class="form-group">
      <%= f.label :nome %>
      <%=f.text_field :nome, class: "form-control", id: "label-form" %>
  </div>

  <div class="form-group">
      <%= f.label :email %>
      <%=f.text_field :email, class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :telefone %>
      <%=f.text_field :telefone, class: "form-control" %>
  </div>

  <div class="form-group">
      <%= f.label :texto %>
      <%=f.text_area :texto, class: "form-control", rows: "4" %>
  </div class = "d-flex justify-content-center">
    <div>
      <%= f.submit "Enviar", class: "btn btn-dark" %>
    </div>
<%end%>
        